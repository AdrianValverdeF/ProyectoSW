<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Apuestas</title>
    <link rel="stylesheet" href="/css/estilo.css">
</head>
<body>
    <main>
        <h1 class="titulo-mis-apuestas">Mis Apuestas</h1>
        <a href="/contenido/eventos" class="btn-volver">← Volver a eventos</a>
        <% if (apuestas && apuestas.length > 0) { %>
            <ul class="apuestas-lista">
                <% apuestas.forEach(apuesta => { %>
                    <li class="apuesta-card">
                        <div class="apuesta-titulo">
                            Partido: <%= apuesta.equipoA %> vs <%= apuesta.equipoB %>
                        </div>
                        <div class="apuesta-header">
                            <span class="apuesta-equipos">
                                <%= apuesta.equipoA %>
                                <span class="apuesta-vs">vs</span>
                                <%= apuesta.equipoB %>
                            </span>
                            <span class="apuesta-fecha">
                                <i class="fa-regular fa-calendar"></i>
                                <%= apuesta.fecha ? new Date(apuesta.fecha).toLocaleDateString() : 'Sin fecha' %>
                            </span>
                        </div>
                        <div class="apuesta-detalles">
                            <span>Cantidad:<strong><%= apuesta.cantidad_apuesta %> €</strong></span>
                            <span>Multiplicador:<strong><%= apuesta.multiplicador %></strong></span>
                            <span>Combinada:<strong><%= apuesta.combinada ? 'Sí' : 'No' %></strong></span>
                        </div>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <div class="apuestas-vacio">
                <p>No tienes apuestas registradas.</p>
            </div>
        <% } %>
    </main>
</body>
</html>